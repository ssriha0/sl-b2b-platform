<%
	response.setHeader("Cache-Control", "no-cache"); //HTTP 1.1
	response.setHeader("Pragma", "no-cache"); //HTTP 1.0
	response.setDateHeader("Expires", 0); //prevents caching at the proxy server
%>
<h2 id="page-role">
	Administrator Office
</h2>

<h2 id="page-title">
	Select Provider Network (SPN)
</h2>

<h3 class="">
	SPN Auditor
</h3>
<p>
	The SPN Auditor automatically displays applicants from two separate
	queues for review and approval. The number shown represents the
	applications that are unlocked and available for review (not the total
	number in the queue). The Search tab offers the ability to find a
	specific firm's information and membership status.
</p>
